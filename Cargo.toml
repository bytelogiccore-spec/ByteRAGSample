[package]
name = "byterag_sample"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.35", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
regex = "1.10"
walkdir = "2.4"
byterag-core = { git = "https://github.com/bytelogiccore-spec/ByteRAG.git", tag = "v0.3.0" }
